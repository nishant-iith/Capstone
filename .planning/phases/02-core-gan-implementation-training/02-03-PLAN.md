---
phase: 02-core-gan-implementation-training
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: [src/data/dataset.py, src/training/trainer.py]
autonomous: false
requirements: []
must_haves:
  truths:
    - "Training loop runs on Kaggle without OOM"
    - "Loss curves show convergence"
  artifacts:
    - path: "src/data/dataset.py"
      provides: "PyTorch Dataset wrapper for aligned patches"
    - path: "src/training/trainer.py"
      provides: "Kaggle execution script"
  key_links:
    - from: "src/data/dataset.py"
      to: "src/training/lightning_module.py"
      via: "DataLoader"
---

<objective>
Wire the data pipeline to the model and execute the training loop on Kaggle.

Purpose: To transition from architectural implementation to a trained baseline model.
Output: A trained model checkpoint (.ckpt) and training logs.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-gan-implementation-training/02-CONTEXT.md
@src/training/lightning_module.py
@src/data/pairing.py
@src/data/splitting.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PyTorch Dataset Wrapper</name>
  <files>src/data/dataset.py</files>
  <action>
    Implement a `VirtualHEDataset(torch.utils.data.Dataset)` that:
    - Loads the paired image paths from the Phase 1 splitting outputs.
    - Loads `.tif` images using an optimized library (e.g., tifffile or OpenCV).
    - Normalizes image pixels to [-1, 1].
    - Returns pairs of (unstained, stained) tensors.
  </action>
  <verify>
    <automated>python -c "from src.data.dataset import VirtualHEDataset; import torch; ds = VirtualHEDataset(); x, y = ds[0]; print(x.shape, y.shape)"</automated>
  </verify>
  <done>Dataset correctly loads and normalizes paired patches</done>
</task>

<task type="auto">
  <name>Task 2: Implement Kaggle Trainer Script</name>
  <files>src/training/trainer.py</files>
  <action>
    Create a script to orchestrate training on Kaggle:
    - Configure PyTorch Lightning `Trainer` with `accelerator='gpu'`, `devices=1`, and `precision='16-mixed'`.
    - Set batch size to 4-8 as per 02-CONTEXT.md.
    - Integrate the `VirtualHEDataset` via `DataLoader`.
    - Add logging (TensorBoard or CSV) to track G/D loss and L1 loss.
  </action>
  <verify>
    <automated>python src/training/trainer.py --dry-run</automated>
  </verify>
  <done>Trainer script is ready for Kaggle execution</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Execute Training on Kaggle</name>
  <what-built>Complete GAN training pipeline (Data + Model + Trainer)</what-built>
  <how-to-verify>
    1. Upload code and dataset to Kaggle.
    2. Run `src/training/trainer.py`.
    3. Monitor loss curves in the console/logger.
    4. Verify that the model does not crash with OOM.
    5. Check that a `.ckpt` file is generated.
  </how-to-verify>
  <resume-signal>Type "approved" once training converges and checkpoint is saved</resume-signal>
</task>

</tasks>

<verification>
- Verify that the training loss is decreasing.
- Confirm that the mixed precision training is actually reducing VRAM usage.
</verification>

<success_criteria>
- Model training completes successfully on Kaggle.
- A weight checkpoint is saved that represents a converging baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-gan-implementation-training/02-03-SUMMARY.md`
</output>
