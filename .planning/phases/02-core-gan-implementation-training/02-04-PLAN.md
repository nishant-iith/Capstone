---
phase: 02-core-gan-implementation-training
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified: [src/inference/predict.py, src/inference/visualize.py]
autonomous: true
requirements: []
must_haves:
  truths:
    - "User can generate a virtual H&E image from an unstained patch"
    - "Side-by-side comparison grids are produced"
  artifacts:
    - path: "src/inference/predict.py"
      provides: "Checkpoint loading and inference logic"
    - path: "src/inference/visualize.py"
      provides: "Visualization utilities for comparison"
  key_links:
    - from: "src/inference/predict.py"
      to: "src/models/gan.py"
      via: "model loading"
---

<objective>
Implement the inference utility to produce and visualize virtual stains.

Purpose: To provide a way to evaluate the baseline model's performance visually.
Output: A script that generates H&E-like images from unstained inputs.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/models/gan.py
@src/training/lightning_module.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Model Inference Script</name>
  <files>src/inference/predict.py</files>
  <action>
    Create a prediction script that:
    - Loads the `VirtualHEGAN` Lightning module and restores weights from a `.ckpt` file.
    - Implements a `predict(image_path)` function that preprocesses the unstained image and runs it through the Generator.
    - Post-processes the output (Tanh $\rightarrow$ [0, 255]) and saves the result as a `.tif` or `.png`.
  </action>
  <verify>
    <automated>python src/inference/predict.py --input test_unstained.tif --checkpoint model.ckpt</automated>
  </verify>
  <done>User can produce a virtual H&E image from a single input file</done>
</task>

<task type="auto">
  <name>Task 2: Create Visual Comparison Utility</name>
  <files>src/inference/visualize.py</files>
  <action>
    Implement a utility to generate qualitative evaluation grids:
    - Function to create a 3-column grid: [Unstained, Virtual H&E, Real H&E].
    - Support for processing a folder of test patches.
    - Save the resulting grids as a composite image for easy review.
  </action>
  <verify>
    <automated>python src/inference/visualize.py --dir test_samples/ --output grid.png</automated>
  </verify>
  <done>Side-by-side grids are generated for visual baseline check</done>
</task>

</tasks>

<verification>
- Verify that the inference script handles image normalization correctly (matching the training pipeline).
- Confirm that the generated images have the expected H&E color profile (pink/purple).
</verification>

<success_criteria>
- A functional pipeline exists to go from .ckpt $\rightarrow$ Unstained Image $\rightarrow$ Virtual H&E Image.
- Visual grids are produced for phase-end review.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-gan-implementation-training/02-04-SUMMARY.md`
</output>
