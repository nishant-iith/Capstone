---
phase: 02-core-gan-implementation-training
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/models/gan.py, src/models/types.py]
autonomous: true
requirements: [MODL-01, MODL-02, MODL-03]
must_haves:
  truths:
    - "Model architecture matches Pix2Pix cGAN specifications"
    - "Generator supports transfer learning via ResNet-34 encoder"
  artifacts:
    - path: "src/models/gan.py"
      provides: "Generator and Discriminator implementations"
  key_links:
    - from: "src/models/gan.py"
      to: "torchvision.models.resnet34"
      via: "encoder initialization"
---

<objective>
Implement the core GAN architecture for virtual H&E staining.

Purpose: To create the neural network structures capable of mapping unstained morphology to H&E textures.
Output: Functional Generator and Discriminator modules.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-gan-implementation-training/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Model Interfaces</name>
  <files>src/models/types.py</files>
  <action>
    Create type definitions and interfaces for the Generator and Discriminator. 
    Define the input/output shapes (e.g., [B, 3, H, W]) to ensure consistency across the training pipeline.
  </action>
  <verify>File exists with exported types</verify>
  <done>Contracts established for model I/O</done>
</task>

<task type="auto">
  <name>Task 2: Implement U-Net Generator with ResNet-34 Encoder</name>
  <files>src/models/gan.py</files>
  <action>
    Implement the Generator as a U-Net architecture per MODL-01 and MODL-03:
    - Encoder: Use a pre-trained ResNet-34 (torchvision) as the backbone. Extract feature maps from each stage.
    - Decoder: Implement symmetric upsampling blocks with strided convolutions and skip connections from the encoder.
    - Normalization: Use Instance Normalization (not BatchNorm) to avoid batch-size dependency.
    - Activation: Use LeakyReLU for all internal layers and Tanh for the final output.
  </action>
  <verify>
    <automated>python -c "from src.models.gan import Generator; import torch; g = Generator(); x = torch.randn(1, 3, 256, 256); print(g(x).shape)"</automated>
  </verify>
  <done>Generator produces output of same shape as input</done>
</task>

<task type="auto">
  <name>Task 3: Implement PatchGAN Discriminator</name>
  <files>src/models/gan.py</files>
  <action>
    Implement the PatchGAN Discriminator per MODL-02:
    - Architecture: A convolutional network that outputs a grid (e.g., 70x70) of validity scores.
    - Input: Concatenate the source unstained image and the target (real or virtual) H&E image along the channel dimension.
    - Layers: Use strided convolutions with LeakyReLU and Instance Normalization.
    - Goal: Prioritize local texture realism (nuclei and cell membranes) rather than global structure.
  </action>
  <verify>
    <automated>python -c "from src.models.gan import Discriminator; import torch; d = Discriminator(); x = torch.randn(1, 3, 256, 256); y = torch.randn(1, 3, 256, 256); print(d(x, y).shape)"</automated>
  </verify>
  <done>Discriminator outputs a feature map of validity scores</done>
</task>

</tasks>

<verification>
- Verify that the Generator output range is [-1, 1] (Tanh).
- Verify that the Discriminator can handle paired inputs.
- Confirm ResNet-34 weights are loaded into the encoder.
</verification>

<success_criteria>
- Model classes are implemented and pass shape verification tests.
- Architecture adheres to the decisions in 02-CONTEXT.md.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-gan-implementation-training/02-01-SUMMARY.md`
</output>
