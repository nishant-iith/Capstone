---
phase: 02-core-gan-implementation-training
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [src/training/lightning_module.py]
autonomous: true
requirements: [TRAIN-01, TRAIN-02, TRAIN-03]
must_haves:
  truths:
    - "Training logic uses LSGAN + L1 loss"
    - "Learning rate scheduling and early stopping are configured"
  artifacts:
    - path: "src/training/lightning_module.py"
      provides: "PyTorch Lightning module for GAN training"
  key_links:
    - from: "src/training/lightning_module.py"
      to: "src/models/gan.py"
      via: "instantiation of Generator and Discriminator"
---

<objective>
Implement the training logic and loss functions for the Virtual H&E GAN.

Purpose: To encapsulate the GAN optimization process into a scalable PyTorch Lightning module.
Output: A `VirtualHEGAN` LightningModule.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-gan-implementation-training/02-CONTEXT.md
@src/models/gan.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement LSGAN and L1 Loss Logic</name>
  <files>src/training/lightning_module.py</files>
  <action>
    Implement the training step in a PyTorch Lightning module:
    - Loss Functions:
      - GAN Loss: Use MSELoss for Least Squares GAN (LSGAN) for both Generator and Discriminator.
      - Structural Loss: Use L1 loss between generated and target H&E images.
    - Optimization:
      - Weight the L1 loss with $\lambda=100$ as per 02-CONTEXT.md.
      - Implement the standard GAN alternating update: update Discriminator, then update Generator.
    - Metrics: Track G_loss, D_loss, and L1_loss.
  </action>
  <verify>
    <automated>python -c "from src.training.lightning_module import VirtualHEGAN; import torch; m = VirtualHEGAN(); x = torch.randn(1, 3, 256, 256); y = torch.randn(1, 3, 256, 256); m.training_step(x, y, 0)"</automated>
  </verify>
  <done>Training step computes losses and performs backprop without errors</done>
</task>

<task type="auto">
  <name>Task 2: Implement LR Scheduling and Early Stopping</name>
  <files>src/training/lightning_module.py</files>
  <action>
    Configure training dynamics per TRAIN-02:
    - Learning Rate: Implement a scheduler (e.g., linear decay to zero) to stabilize training.
    - Early Stopping: Implement a callback to stop training when validation L1 loss plateaus.
    - Optimizer: Use Adam with $\beta_1=0.5$ and $lr=2e-4$.
  </action>
  <verify>
    <automated>python -c "from src.training.lightning_module import VirtualHEGAN; m = VirtualHEGAN(); assert m.configure_optimizers() is not None"</automated>
  </verify>
  <done>Optimizer and scheduler are correctly configured in the Lightning module</done>
</task>

<task type="auto">
  <name>Task 3: Implement Checkpointing System</name>
  <files>src/training/lightning_module.py</files>
  <action>
    Implement the checkpointing strategy per TRAIN-03:
    - Use PyTorch Lightning's `ModelCheckpoint` callback.
    - Save the "best" model based on minimum validation L1 loss.
    - Ensure both Generator and Discriminator weights are saved.
  </action>
  <verify>
    <automated># Manual verification that ModelCheckpoint is instantiated and passed to trainer</automated>
  </verify>
  <done>Checkpoint system configured to save best model based on val loss</done>
</task>

</tasks>

<verification>
- Confirm that G and D are updated independently.
- Verify that the L1 loss is dominating the early training phase.
- Check that the optimizer parameters match the decisions in 02-CONTEXT.md.
</verification>

<success_criteria>
- `VirtualHEGAN` module is implemented and passes a dummy training step.
- Loss functions and hyperparameters are correctly aligned with project decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-gan-implementation-training/02-02-SUMMARY.md`
</output>
