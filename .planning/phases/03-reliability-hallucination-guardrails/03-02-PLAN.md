---
phase: 03-reliability-hallucination-guardrails
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/training/lightning_module.py
autonomous: true
requirements:
  - MODL-04
must_haves:
  truths:
    - "Pix2PixLightning accepts lambda_struct hyperparameter in __init__"
    - "Training step computes three loss terms: L_LSGAN, L_L1, L_struct"
    - "All three loss terms are logged to tensorboard/console separately"
    - "Generator loss combines all three: g_loss = g_gan_loss + lambda_l1*g_l1_loss + lambda_struct*g_struct_loss"
  artifacts:
    - path: "src/training/lightning_module.py"
      provides: "Updated Pix2PixLightning with structural loss integration"
      contains: "StructuralConsistencyLoss" and "lambda_struct" and "g_struct_loss"
    - path: "src/training/lightning_module.py"
      provides: "Logging for three loss terms"
      exports: ["g_gan_loss", "g_l1", "g_struct"]
  key_links:
    - from: "Pix2PixLightning.__init__"
      to: "StructuralConsistencyLoss"
      via: "instantiation"
      pattern: "self.criterion_struct = StructuralConsistencyLoss\\(\\)"
    - from: "training_step"
      to: "three loss computations"
      via: "sequential loss calculation"
      pattern: "g_gan_loss.*g_l1_loss.*g_struct_loss"
    - from: "self.log"
      to: "g_struct"
      via: "tensorboard logging"
      pattern: "self.log.*g_struct"
---

<objective>
Update Pix2PixLightning to integrate structural loss as a third term in the generator loss function, enabling hallucination suppression during training.

Purpose: Per MODL-04 requirement and D-01 decision, the loss function must combine three terms: `Loss = L_LSGAN + λ1*L1 + λ_struct*L_struct`. This requires modifying the existing lightning module to import and use the StructuralConsistencyLoss from Plan 01.

Output: Updated Pix2PixLightning module that trains with three weighted loss terms and logs each separately for monitoring.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-reliability-hallucination-guardrails/03-CONTEXT.md
@.planning/phases/03-reliability-hallucination-guardrails/03-01-PLAN.md

Phase 2 Baseline (current code in src/training/lightning_module.py):
- Training step computes: d_loss, g_gan_loss, g_l1_loss
- Generator loss: g_loss = g_gan_loss + lambda_l1 * g_l1_loss
- Hyperparameters: lr=2e-4, lambda_l1=100

Phase 3 Changes Needed:
- Add new hyperparameter: lambda_struct=10 (per D-02)
- Compute g_struct_loss from StructuralConsistencyLoss
- Update g_loss = g_gan_loss + lambda_l1 * g_l1_loss + lambda_struct * g_struct_loss
- Log g_struct separately (per D-04: monitor all three terms)

Existing Patterns:
- self.criterion_gan = nn.MSELoss() (LSGAN)
- self.criterion_l1 = nn.L1Loss()
- self.log("metric_name", value, prog_bar=True/False)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Pix2PixLightning to integrate StructuralConsistencyLoss and log three loss terms</name>
  <files>src/training/lightning_module.py</files>
  <read_first>
    - src/training/lightning_module.py (current Phase 2 implementation)
    - src/training/structural_loss.py (from Plan 01, to understand StructuralConsistencyLoss interface)
  </read_first>
  <action>
Modify src/training/lightning_module.py with the following changes:

1. **Import StructuralConsistencyLoss:**
   - Add line: `from src.training.structural_loss import StructuralConsistencyLoss`

2. **Update __init__ signature:**
   - Change from: `def __init__(self, lr=2e-4, lambda_l1=100):`
   - To: `def __init__(self, lr=2e-4, lambda_l1=100, lambda_struct=10):`
   - Inside __init__, after existing criterion instantiations, add:
     ```python
     self.criterion_struct = StructuralConsistencyLoss()
     ```
   - Keep existing `self.gen` and `self.disc` instantiations unchanged

3. **Update training_step method:**
   - Keep discriminator training unchanged (no structural guidance per D-03)
   - In generator training section, after computing `g_l1_loss`, add:
     ```python
     g_struct_loss = self.criterion_struct(fake_stained, stained)
     ```
   - Update generator loss computation from:
     ```python
     g_loss = g_gan_loss + self.hparams.lambda_l1 * g_l1_loss
     ```
     To:
     ```python
     g_loss = g_gan_loss + self.hparams.lambda_l1 * g_l1_loss + self.hparams.lambda_struct * g_struct_loss
     ```

4. **Update logging (per D-04 requirement to monitor all three terms):**
   - Keep existing: `self.log("d_loss", d_loss, prog_bar=True)`
   - Keep existing: `self.log("g_loss", g_loss, prog_bar=True)`
   - Keep existing: `self.log("g_l1", g_l1_loss, prog_bar=False)`
   - Add new: `self.log("g_struct", g_struct_loss, prog_bar=True)` (on progress bar for monitoring)
   - Optionally add: `self.log("g_gan", g_gan_loss, prog_bar=False)` for debugging

5. **Preserve validation step behavior:**
   - If validation_step exists, leave unchanged (Phase 2 may not have validation loss, but don't break it)
   - If validation_step doesn't exist, no changes needed

6. **Documentation:**
   - Update class docstring to mention: "Implements LSGAN + L1 + Structural Consistency losses"
   - Update lambda_struct parameter docstring: "Weight for structural consistency loss (default 10, ~10% of L1's contribution)"

**Key Implementation Details:**
- Do NOT change discriminator forward or loss calculation (D-03: keep discriminator unchanged)
- All three losses are computed from fake_stained output of generator
- Structural loss uses same fake_stained tensor as L1 loss (no redundant generation)
- Batch dimensions preserved through all computations
- Return statement unchanged (return {"loss": g_loss})
  </action>
  <verify>
    <automated>python -c "from src.training.lightning_module import Pix2PixLightning; m = Pix2PixLightning(lambda_struct=10); import inspect; sig = inspect.signature(m.__init__); assert 'lambda_struct' in sig.parameters, 'lambda_struct param missing'; assert hasattr(m, 'criterion_struct'), 'criterion_struct missing'; print('Pix2PixLightning updated successfully')"</automated>
  </verify>
  <acceptance_criteria>
    - File src/training/lightning_module.py imports StructuralConsistencyLoss
    - Pix2PixLightning.__init__ accepts lambda_struct parameter with default value 10
    - Pix2PixLightning has attribute criterion_struct of type StructuralConsistencyLoss
    - training_step computes g_struct_loss via self.criterion_struct(fake_stained, stained)
    - Generator loss combines three terms: g_gan_loss + lambda_l1*g_l1_loss + lambda_struct*g_struct_loss
    - All three losses logged: g_gan_loss (or g_gan), g_l1, g_struct
    - Discriminator training unchanged (no structural guidance)
    - Module structure preserves backward compatibility (can still instantiate with old hyperparameters)
  </acceptance_criteria>
  <done>
    - Pix2PixLightning supports three-term loss function with configurable weights
    - All three loss components monitored via tensorboard/console logging
    - Ready for fine-tuning Phase 2 checkpoint in Plan 03
  </done>
</task>

</tasks>

<verification>
Verify that:
1. Updated module imports without errors
2. Can instantiate Pix2PixLightning with default and custom lambda_struct values
3. training_step method computes all three loss components
4. Logging includes g_struct term
5. Generator loss is weighted sum of three terms
</verification>

<success_criteria>
- Pix2PixLightning is updated and ready for training with three loss terms
- Loss computation follows D-01 decision: L_LSGAN + λ1*L1 + λ_struct*L_struct
- All three terms logged per D-04 requirement
</success_criteria>

<output>
After completion, create `.planning/phases/03-reliability-hallucination-guardrails/03-02-SUMMARY.md`
</output>
