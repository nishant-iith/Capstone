---
phase: 01-data-engineering-registration
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [src/data/splitting.py, src/data/augmentation.py]
autonomous: false
requirements: [DATA-03, DATA-04]
must_haves:
  truths:
    - "No patient ID is shared between train, validation, and test sets"
    - "Training set is augmented while preserving anatomical plausibility"
  artifacts:
    - path: "src/data/splitting.py"
      provides: "Patient-level splitting logic"
    - path: "src/data/augmentation.py"
      provides: "Symmetric augmentation pipeline"
  key_links:
    - from: "src/data/registration.py"
      to: "src/data/splitting.py"
      via: "Aligned dataset"
      pattern: "data/processed/"
    - from: "src/data/splitting.py"
      to: "src/data/augmentation.py"
      via: "Training split"
      pattern: "data/final/train"
---

<objective>
Finalize the dataset by performing a leak-free patient-level split and expanding the training set via a robust augmentation pipeline.

Purpose: Splitting by patient prevents the model from "memorizing" specific tissue textures from the same slide, and augmentation prevents overfitting on small medical datasets.
Output: Final structured dataset in `data/final/` (train, val, test).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-engineering-registration/01-CONTEXT.md
@.planning/ROADMAP.md
@.planning/phases/01-data-engineering-registration/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Patient-Level Splitting</name>
  <files>src/data/splitting.py</files>
  <behavior>
    - Input: Aligned image pairs from `data/processed/`.
    - Logic: Extract Patient IDs from filenames. Split IDs into Train (80%), Val (10%), and Test (10%).
    - Output: Files organized into `data/final/train/`, `data/final/val/`, and `data/final/test/`.
    - Constraint: ZERO overlap of Patient IDs between sets.
  </behavior>
  <action>
    Create `src/data/splitting.py`. Implement a `split_dataset()` function that performs the ID-based split and moves the paired images into the final directory structure.
  </action>
  <verify>
    <automated>python src/data/splitting.py --verify-leakage</automated>
  </verify>
  <done>Dataset split into three sets with guaranteed zero patient leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Symmetric Augmentation Pipeline</name>
  <files>src/data/augmentation.py</files>
  <action>
    Create `src/data/augmentation.py`. Implement an augmentation pipeline for the training set:
    1. Use `albumentations` or `torchvision`.
    2. Apply synchronized transformations to BOTH the unstained and stained images in a pair (to maintain alignment).
    3. Include: 90/180/270 degree rotations, horizontal and vertical flips, and slight elastic deformations.
    4. Save augmented pairs to `data/final/train_aug/`.
  </action>
  <verify>
    <automated>python src/data/augmentation.py --test-sample</automated>
  </verify>
  <done>Augmented training set generated and verified as anatomically plausible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Final Dataset Structure and Augmentations</what-built>
  <how-to-verify>
    1. Check `data/final/` to verify the existence of train, val, and test folders.
    2. Verify that the number of images in each set matches the 80/10/10 split.
    3. Visually inspect 5 augmented pairs in `data/final/train_aug/` to ensure rotations/flips are synchronized.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- No Patient ID is present in more than one split.
- Augmented images maintain the same spatial relationship as the originals.
- Final folder structure is compatible with a PyTorch Dataset loader.
</verification>

<success_criteria>
A fully prepared, leak-free, and augmented dataset ready for input into the Phase 2 GAN training loop.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-engineering-registration/01-02-SUMMARY.md`
</output>
